export type TViewMode = "grid" | "list";
export type TSearchMode = "basic" | "advanced";

export type TAdvancedFilters = {
  name: string;
  email: string;
  phone: string;
  skype: string;
  building: string;
  room: string;
  department: string;
};

